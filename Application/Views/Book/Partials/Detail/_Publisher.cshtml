@model Core.Models.Catalog.Publisher

<div class="row">
    <hr class="my-3"/>
    <h3>Publisher Details</h3>
    <div class="col-md-4">
        <dt>Name</dt>
        <dd>@Model.Name</dd>
    </div>
    <div class="col-md-4">
        <dt>Books</dt>
        <dd>@Model.Books.Count</dd>
    </div>
</div>